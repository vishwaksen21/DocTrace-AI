apiVersion: v1
kind: Service
metadata:
  name: {{ include "doctrace-ai.fullname" . }}
  labels:
    {{- include "doctrace-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.service.port | default 80 }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "doctrace-ai.selectorLabels" . | nindent 4 }}