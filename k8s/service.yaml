apiVersion: v1
kind: Service
metadata:
  name: doctrace-api
  namespace: doctrace-ai
  labels:
    app: doctrace-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: doctrace-api